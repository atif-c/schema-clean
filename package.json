{
	"name": "schema-clean",
	"version": "1.0.1",
	"description": "Recursively clean objects and arrays against a template — removes unknown keys, adds missing defaults, and resets invalid types",
	"author": "Atif C <atifcdev@gmail.com>",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/atif-c/schema-clean.git"
	},
	"bugs": {
		"url": "https://github.com/atif-c/schema-clean/issues"
	},
	"homepage": "https://github.com/atif-c/schema-clean#readme",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		}
	},
	"sideEffects": false,
	"files": [
		"dist"
	],
	"keywords": [
		"clean",
		"sanitise",
		"sanitize",
		"object",
		"array",
		"template",
		"recursive",
		"validate",
		"schema",
		"defaults",
		"typescript"
	],
	"scripts": {
		"build": "tsc",
		"test": "vitest run",
		"test:watch": "vitest --watch",
		"test:coverage": "vitest run --coverage",
		"prettier:check": "prettier --check .",
		"prettier:write": "prettier --write .",
		"eslint:check": "eslint .",
		"eslint:fix": "eslint --fix .",
		"format": "prettier --write . && eslint --fix .",
		"prepublishOnly": "pnpm build"
	},
	"devDependencies": {
		"@eslint/js": "^10.0.1",
		"@vitest/coverage-v8": "^5.0.0",
		"eslint": "^10.10.0",
		"prettier": "^3.9.6",
		"typescript": "^7.0.2",
		"typescript-eslint": "^8.70.0",
		"vitest": "^5.0.0"
	}
}
